import React from 'react';
import loader from "../loader.gif"

const Loader = () => {
    return (
        <div>
            <img src={loader} alt="Loading..."/>
        </div>
    );
};

export default Loader;